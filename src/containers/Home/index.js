import React from 'react'
import Header from '../../components/Header'

export const Home = (props) => {
    return (
        <div>
            <p>Home Page </p>
            <Header />
        </div>
    )
}

export default Home;
