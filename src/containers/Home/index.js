import React from 'react'
import Header from '../../components/Header'

export const Home = (props) => {
    return (
        <div>
            <Header />
        </div>
    )
}

export default Home;
